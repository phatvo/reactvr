# Fix cửa 4.5m và chiều drag chuột

Bản này cập nhật:

- Cửa phòng trưng bày rộng 4.5m.
- Đảo chiều kéo chuột trên PC bằng `controls.rotateSpeed = -0.65`.
- Giữ nguyên chiều drag ngón tay trên điện thoại.
- Giữ nguyên các fix trước: phòng 20m x 20m x 6m, cảm biến điện thoại, click-to-move, touch giữ vị trí, fix ảnh bị flicker khi di chuyển.

Cập nhật GitHub Pages:

```bash
git add .
git commit -m "Increase door width and fix mouse drag direction"
git push
```
